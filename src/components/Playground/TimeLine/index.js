import React from "react";

const TimeLine = () => {
  return (
    <div
      className="h-full"
      style={{
        background: "linear-gradient(180deg, #26001A 0%, #160015 100%)",
      }}
    ></div>
  );
};

export default TimeLine;
